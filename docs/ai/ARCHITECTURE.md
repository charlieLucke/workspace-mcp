# Architecture

> System-level design. Update when modules, contracts, or data models change.

## Overview
*(One paragraph: what does this project do, at the system level?)*

## Module map
*(High-level modules and their responsibilities. Example below.)*

```
src/workspace_mcp/
├── api/          # HTTP/CLI interface layer
├── core/         # Domain logic
├── adapters/     # External service clients (DB, APIs)
├── config/       # Configuration loading
└── main.py       # Entry point
```

## Data model
*(Key entities and their relationships. ERD or prose.)*

## External services
*(APIs, databases, message queues, file storage, etc. List each with purpose.)*

## Data flow
*(How does data move through the system? End-to-end for the main use case.)*

## Deployment
*(Where and how this runs. Local, cloud, container, serverless.)*
